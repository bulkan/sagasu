const searchService = require('./searchService');

describe('searchService', () => {
	describe('#list', () => {
		it.todo('should list all available fields');
	});

	describe('#query', () => {
		it.todo('should return all results');
	});
});