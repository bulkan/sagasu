#!/usr/bin/env node

const { handleCli } = require('../src/cli');

function main() {
	handleCli(process.argv)
		.then(result => {
			console.log(result);
		})
		.catch(err => {
			console.error(err);
			process.exit(1);
		});
}

if (require.main === module) {
  main();
}